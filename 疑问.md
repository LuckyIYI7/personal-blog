### 1. 为什么在 E:\Projects\personal-blog 里执行初始化站点骨架的操作？

核心区别是：`Hugo 程序` 和 `Hugo 项目` 是两件不同的事。

1. `E:\DevTools\Hugo`：放的是工具本体（`hugo.exe`）
- 类似“编译器安装目录”
- 全局可复用，多个项目都能用它
- 不应和某个具体博客项目绑死

2. `E:\Projects\personal-blog`：放的是你的网站源码
- `content/`、`layouts/`、`hugo.toml` 等都在这里
- `hugo new site`、`hugo server` 这类命令要在“项目根目录”运行
- 因为 Hugo 会读取“当前目录下的站点结构和配置”

所以流程是：
- 在任何地方安装 Hugo（全局一次）
- 在具体项目目录里使用 Hugo（每个项目都这样）

你之前“全局 PowerShell”操作用于安装和验证，这是正确的。  
现在让你切到 `E:\Projects\personal-blog`，是为了操作站点源码本身，也是正确的工程习惯。

下一步你可以这样做（先切目录再初始化）：
```powershell
cd E:\Projects\personal-blog
hugo new site . --force --format toml
Get-ChildItem
```

### 2. 连接成功但页面显示 `Page Not Found`，这是正常的吗？

是可能正常的，尤其在“刚初始化 Hugo、还没加主题或没写首页模板”的阶段。

根本原因通常是：
- 服务器已经启动（所以能连接）
- 但根路径 `/` 没有可渲染的首页模板（home layout）

如何区分：
- 连接失败：浏览器连不上端口，属于服务/端口问题
- `Page Not Found`：服务在线，但路由或模板缺失，属于渲染/结构问题

下一步排查思路：
- 先确认是否已有主题
- 或者在 `layouts` 中补最小首页模板
- 直接访问文章路径（例如 `/posts/hello-hugo/`）判断内容页是否可用

### 3. 使用 Typora 写 Markdown 后，还需要 `title/date/draft` 吗？

可以用 Typora，完全没问题。但建议保留 front matter（文件头元数据）。

建议至少保留：
- `title`：页面标题
- `date`：发布时间和排序依据
- `draft`：是否草稿（会影响是否在默认模式下展示）

为什么建议保留：
- Hugo 会优先读取 front matter
- 缺失字段时 Hugo 可能做推断，但结果不一定符合预期
- 对学习和排错来说，显式字段更可控、更好调试

推荐工作流：
1. 用 `hugo new content posts/xxx.md` 先生成标准头部
2. 用 Typora 编辑正文
3. 用 `hugo server -D` 本地预览

### 4. 如果文章时间是未来，Hugo 会“定时发布”吗？

结论：Hugo 不会在本机自动“等到时间就发布”，它只会在“构建时”判断要不要包含这篇文章。

规则是：
- 当 `date` 或 `publishDate` 晚于当前构建时间时，这篇文章默认不被发布
- 本地预览时可用 `-F` 包含未来内容：`hugo server -D -F`
- 生产构建时可用 `hugo --buildFuture` 包含未来内容（一般不建议长期开启）

为什么看起来像“定时发布”：
- 如果你的部署平台会按计划自动重新构建（例如每天/每小时触发 CI）
- 当触发构建时，时间已到，文章就会出现在新构建产物里
- 所以“定时发布”本质是“定时重新构建”，不是 Hugo 常驻进程自动发布

### 5. 是否应该在 `AGENTS.md` 里增加“自动记录报错/疑问”规则？

建议增加，而且这是很好的工程习惯。

原因：
- 将“记录行为”流程化，避免每次口头提醒
- 让协作标准可复用，后续排错和复盘效率更高
- 对新手学习尤其有帮助，能沉淀自己的知识库

实践建议：
- 报错记录统一写入 `报错日志.md`
- 非报错疑问统一写入 `疑问.md`
- 采用追加方式，不覆盖历史内容

### 6. `hugo --printUnusedTemplates` 输出里的 `terms.html is unused` 是异常吗？

先看结论：当前不是阻断性异常，站点已正常构建，文章页和标签页可生成。

如何读这段输出：
- `Pages = 16`：本次总共生成了 16 个页面（含首页、文章页、列表页、taxonomy 页、RSS、sitemap 等）
- `Static files = 0`：当前 `static/` 下没有需要拷贝的静态资源
- `WARN Template ... is unused`：这是在启用 `--printUnusedTemplates` 时给出的“调试提示”，不等于构建失败

为什么会看到这个 warning 但页面仍正常：
- 通过 `--templateMetrics` 可见 `_default/terms.html` 实际被执行
- 说明该 warning 属于“模板分析噪声/别名路径提示”，不是页面不可用错误

实践建议：
- 日常开发用 `hugo server -D` 或 `hugo` 即可
- 仅在排查模板匹配问题时，再临时加 `--printUnusedTemplates`

### 7. `New-Item` 和 `notepad` 为什么好用？

结论：这两个命令组合非常适合 Hugo 学习阶段，能把“创建结构 + 快速编辑”连成一个高效动作。

`New-Item` 的价值：
- 在命令行里快速创建目录或文件
- 配合 `-Force` 可以减少“目录已存在”的中断
- 对搭建 Hugo 的 `content/layouts/static` 结构很高效

常用写法：

```powershell
New-Item -ItemType Directory -Path .\content\archives 
New-Item -ItemType File -Path .\content\posts\demo.md
```

高频参数：

- -ItemType：Directory 或 File
- -Path：目标路径
- -Force：目标已存在时不报错（目录场景特别常用）

`notepad` 的价值：
- 直接从终端打开并编辑目标文件
- 不需要手动点开层层目录再双击文件
- 非常适合修改小模板和配置文件

常用写法：

```powershell
notepad .\hugo.toml
notepad .\layouts\_default\single.html
```

推荐组合：
1. `New-Item` 先搭好目录/文件
2. `notepad` 立刻打开编辑
3. `hugo server -D` 立即验证

### 8. 为什么可以先 `pwd + Get-ChildItem`？

结论：这是最低成本、最高收益的“环境确认动作”，能显著减少路径错误和误操作。

`pwd` 的作用：
- 明确当前工作目录
- 防止在错误目录执行 Hugo 命令（这是新手最常见错误之一）

`Get-ChildItem` 的作用：
- 快速查看当前目录结构和关键文件是否存在
- 能马上确认 `hugo.toml`、`content/`、`layouts/` 是否在预期位置

为什么建议先做这两步：
- 比报错后再排查更省时间
- 能在执行破坏性较低的前提下先校验上下文
- 形成稳定的工程习惯：先确认现场，再执行命令

### 9. 项目根目录里的 `codex安装.md` 能直接作为第二篇 Hugo 博文吗？测试文该用真实文章还是简单文章？

结论：
- 这篇内容完全可以作为第二篇博文，而且很适合做“真实测试文”
- 但当前放在项目根目录不会被 Hugo 当作文章渲染，需放到 `content/posts/` 并补齐 front matter

原因：
- Hugo 默认只处理 `content/` 下的内容文件
- 一篇“真实长文”能更好测试标题层级、代码块、链接、归档、标签分类等场景

推荐策略（学习导向）：
- 主线用真实文章（如 `codex安装.md`）做功能验证
- 备用保留一篇简单 `hello` 文章做快速回归测试
- 这样既有真实场景，也有低成本排障样本

### 10. 正文表格不好看，应该立即改还是后面统一改样式？

结论：两种都可以，但更推荐“先最小可读，再统一美化”。

建议实践：
- 当表格已影响阅读时，先补一版基础表格样式（边框、间距、斑马纹、横向滚动）
- 后续再做全站视觉统一时，把表格、代码块、提示框一起进行风格收敛

这样做的好处：
- 先解决可用性，不阻塞内容创作
- 后续还能保留统一改版空间，避免过早设计

### 11. 博客上线后再改样式或修 bug，会不会很麻烦？

结论：不麻烦，前提是把发布流程工程化（你已经在做这件事）。

为什么不麻烦：
- Hugo 是静态站点，修改后只需重新构建并部署 `public/`
- 代码与内容分离，样式改动主要在 `layouts/` 与 `static/`
- 有 `DEPLOYMENT.md` 作为 SOP，可重复执行

建议做法：
1. 改动前先做小范围预览（`hugo server`）
2. 通过后再跑生产构建（`hugo --gc --minify`）
3. 再发布，避免“线上直接试错”

### 12. 看到喜欢的 GitHub Hugo 主题，如何应用？

推荐流程（可维护）：
- 优先用 Hugo Module 引入主题（比直接拷贝文件更易升级）
- 先“并行预览”主题，再决定是否全面切换
- 必须保留你现有的自定义覆盖逻辑（`layouts/` 优先级高于主题）

主题接入常见坑：
- 忘记看主题 README 的必需参数（菜单、语言、markup）
- 直接覆盖 `hugo.toml` 导致现有配置丢失
- 不理解模板查找顺序，误以为主题“覆盖不了”或“被覆盖错位”

### 13. 主题不同，迁移难度会不同吗？在 `hugo-theme-void` 和 `neopost` 之间怎么选？

结论：会有差异，但都可以迁移成功。

对比要点：
- `hugo-theme-void`：
  - 功能更“工程化”（TOC、锚点高亮、代码块增强、KaTeX）
  - 若要深度改样式，README 提到需要 Node.js/npm 参与 Tailwind 开发
  - 与当前已做的博客能力（TOC、代码样式）方向更接近，迁移阻力较小
- `neopost`：
  - 视觉风格更强（Neocities 风）
  - 主题结构包含 `sidebar`、`welcome-header` 等内容入口，需要按主题模型补内容
  - 无需前端构建链即可先跑起来，主题色切换也比较直接

推荐策略：
- 若你最看重“视觉风格”并且已经明确偏好 `neopost`，就选 `neopost`
- 若你最看重“最小迁移成本 + 延续当前工程能力”，`void` 会更稳

### 2026-05-23

#### 问题
是否要把这次报错也记录到 报错日志.md？

#### 结论
要记录。凡是影响流程推进的报错（即使最后已解决）都建议记录，便于后续复盘和快速排查。

#### 原因
- 保留“现象 -> 根因 -> 修复”链路，后面遇到同类问题可以秒级定位
- 形成可复用的排障模板，降低重复踩坑成本
- 与 AGENTS.md 中的 Learning Log 规则保持一致

### 2026-05-23

#### 问题
切换到主题后，之前自定义的功能（如文章 TOC、平滑滚动、scroll spy）不见了，是否正常？能否“主题 + 自定义功能”同时保留？

#### 结论
这是正常现象，而且完全可以同时保留。Hugo 的模板优先级是“项目目录优先于主题目录”，所以可以在项目里按需覆盖主题模板，实现“以主题为底座 + 局部自定义增强”。

#### 原因
- 当前为了验证 
eopost，把原 layouts/ 暂时移走，主题模板全面接管渲染
- 之前的 TOC、平滑滚动、scroll spy 都属于项目自定义模板/样式/脚本能力，不在 
eopost 默认功能里

#### 推荐做法
- 不要整站回退到旧布局，而是按功能逐项迁移：
  1. 先恢复文章页 TOC（layouts/_default/single.html 局部覆盖）
  2. 再恢复 static/js/toc-spy.js 与对应 CSS
  3. 每做一项就 hugo server 验收，确保问题可定位

### 2026-05-23

#### 问题
如果后续改成“你来执行，我来查看结果”的推进方式，需要修改 AGENTS.md 吗？

#### 结论
不是必须，但强烈建议写进 AGENTS.md。这样后续每次协作都会自动遵循该节奏，减少反复确认成本。

#### 处理
已在 AGENTS.md 追加 Execution Preference：默认由助手执行实现、用户验收；遇到高风险操作再暂停确认。

### 2026-05-23

#### 现象
- 迁移 neopost 后，TOC 出现了不希望展示的“安装关键节点”（原本不应进入目录）
- 点击“附：一个误区澄清”时体验异常（会停留在上一节高亮）
- 踩坑提醒（note warn）视觉样式丢失

#### 原因
- TOC 级别范围过宽（包含了 h3）且自动编号开启
- 	imeline shortcode 使用了标题标签，导致被 TOC 收录
- scroll spy 对“页面底部最后一个章节”未做特殊处理
- 迁移到主题后未同步旧样式中的 
ote/timeline 视觉规则

#### 解决方案
1. content/posts/_index.md 调整为：	oc-auto-numbering = false，并设置 	oc.endLevel = 2
2. layouts/shortcodes/timeline.html 将时间线标题从 h3 改为 p，避免进入 TOC
3. static/js/toc-spy-neopost.js 增加：
   - 点击 TOC 链接时自定义平滑滚动定位
   - 页面接近底部时自动高亮最后一个章节
4. static/css/neopost-overrides.css 补回 
ote/timeline 的样式体系

#### 结论
- 现在 TOC 仅保留主章节（前言 / 一 / 二 / 三 / 附）
- 目录点击定位更稳定，底部章节高亮正确
- 踩坑提醒 和时间线样式恢复

### 2026-05-23

#### 问题
迁移到 neopost 后，文章里的表格样式又变回默认样式，不如之前精修版好看，如何恢复并兼顾移动端？

#### 结论
通过项目级覆盖恢复表格样式最稳妥：不改主题源码、不改 Markdown 内容，仅在项目层补样式与轻量增强脚本。

#### 处理
1. 在 static/css/neopost-overrides.css 增加表格视觉规则（边框、表头背景、斑马纹、间距）。
2. 新增 static/js/article-enhance.js，自动为文章内 	able 包裹 .table-scroll，实现窄屏横向滚动。
3. 在 layouts/_default/single.html 挂载该脚本。
4. 构建验证通过，neopost 外观保留，表格可读性与移动端体验恢复。

### 2026-05-23

#### 问题
侧栏导航是否应改为更紧凑的双列布局，并移除模板自带外链（itch.io、patreon）？

#### 结论
建议这样做。对个人博客而言，双列能减少留白、提高导航信息密度；移除无关外链可让侧栏更聚焦站内内容。

#### 处理
1. 侧栏链接改为站内 5 项：首页/文章/归档/标签/分类。
2. 删除 itch.io 与 patreon。
3. 新增项目级覆盖样式，让侧栏导航按 2 列网格展示，并保留移动端可读性。
4. 将 
eopost-overrides.css 改为全站加载，确保首页/归档/标签/分类统一生效。

### 2026-05-23

#### 问题
想把模板自带的欢迎文案改成不暴露身份、以猫咪口吻欢迎访客；若用中文则标题也应同步替换。

#### 结论
已改为英文猫咪口吻版本，避免中文编码风险，同时满足“欢迎每位访客、不过度暴露身份”的目标。

#### 处理
- 修改 content/welcome-header/_index.md：
  - 标题改为 meow~ welcome to this cozy corner
  - 两段介绍改为欢迎访客、说明博客内容方向
  - 小列表改为 learning notes / code experiments / pitfalls and fixes
- 构建验证通过。

### 2026-05-23

#### 问题
- 点击“文章”页时内容空白。
- 点击“标签/分类”后内容左对齐，没有像归档页一样居中。

#### 原因
- /posts/ 没有项目级 section 模板，回退到主题默认 _default/list.html（该模板是空实现）。
- 标签/分类模板缺少 #single-post 居中容器。

#### 处理
1. 新增 layouts/posts/section.html：恢复文章列表页（按时间倒序）。
2. 重写 layouts/tags/*.html 与 layouts/categories/*.html：统一加 #single-post 包裹，保持居中布局。
3. 构建验证通过，相关页面已正常显示且居中。

### 2026-05-23

#### 问题
Hugo 是否支持“删除博文 + 回收站 + 二次确认 + 恢复后发布时间不变”的完整机制？

#### 结论
可以实现，但 Hugo 本身是静态站点生成器，不带可写后端，所以“点击删除并写回文件”这类动作不能仅靠前台模板完成。要实现完整流程，需要配合 Git 工作流、脚本工具，或接入 CMS（如 Decap CMS）。

#### 实现建议（推荐顺序）
1. 软删除（回收站）：将文章从 content/posts/ 移到 content/trash/，或加 	rashed = true 并从列表中过滤。
2. 恢复：移回 content/posts/（或去掉 	rashed），保留原 front matter 的 date，发布时间自然不变。
3. 永久删除：仅在回收站执行物理删除。
4. 二次确认：在脚本/CMS 操作层做确认弹窗（Hugo 模板本身无法直接改本地文件）。

#### 额外提醒
- 已发布文章删除后会变成 404，建议配合 301 跳转或自定义 404 提示。
- 最稳妥的“回收站”其实是 Git 历史：可随时恢复任何版本。

### 2026-05-23

#### 问题
想做“删除 -> 回收站 -> 恢复/彻底删除”的工作流，并且回收站应只在本地可见，部署后访客不可见。

#### 结论
已实现可用版本：
1. 本地可见回收站页面（`content/trash/` + `layouts/trash/section.html`）。
2. 侧栏回收站链接支持 `localOnly: true`，仅 development 环境显示。
3. 提供一键脚本 `scripts/trash.ps1`，含确认提示：`list/move/restore/purge/purge-all`。
4. 生产构建默认隐藏回收站链接，且不生成回收站页面。

#### 使用入口
- 文档：`TRASH_WORKFLOW.md`
- 命令：`powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\trash.ps1 <action> [target]`

### 2026-05-23

#### 问题
如果想手动删除博文，是否直接在 `content` 下删除对应 Markdown 文件即可？

#### 结论
可以。最直接是删除 `content/posts/<slug>.md`，重新构建后文章就不会再出现在站点中。

#### 建议
1. 已发布文章不建议直接硬删，优先使用回收站工作流：`scripts/trash.cmd move <slug>`。
2. 真要硬删也建议先 `git commit` 或至少备份，避免误删无法恢复。
3. 删除后若有外链访问旧 URL，会变成 404，后续可视情况补重定向。

### 2026-05-23

#### 问题
回收站功能当前带来副作用，是否可以先移除，后续再做扩展？

#### 结论
可以，且这是合理取舍。先保证博客雏形稳定，再迭代扩展功能，能显著降低排错成本。

#### 处理
- 已移除回收站相关文件与页面（`content/trash`、`layouts/trash`、`scripts/trash*`、`TRASH_WORKFLOW.md`）。
- 已移除侧栏“回收站”入口与 `localOnly` 逻辑。
- 首页 TOC 按钮异常已修复。

## 2026-05-24 疑问：为什么昨天新建并推送后，GitHub 看不到仓库？

- 时间：2026-05-24
- 现象：本地以为已新建并推送，但 GitHub 网页看不到对应仓库/代码。
- 排查：
  - `git remote -v` 结果为空（当前仓库没有配置任何远端）。
  - 当前分支 `feat/theme-neopost-migration` 有提交记录，说明代码在本地，但没有远端映射。
- 原因结论：当前目录下这个 Git 仓库并未成功绑定 `origin` 并执行有效 push（或曾在其他目录/其他账号操作）。
- 解决方案：
  1. 在 GitHub 新建空仓库（确认登录的是目标账号）。
  2. 本地执行 `git remote add origin <仓库URL>`。
  3. 执行 `git push -u origin feat/theme-neopost-migration`。
  4. 用 `git remote -v`、`git branch -vv`、`git ls-remote --heads origin` 三步确认。
- 常见误区：
  - 在错误目录执行了 `git push`。
  - GitHub 登录的是另一个账号或组织空间。
  - 只执行了 `git init/git commit`，漏掉 `remote add` 和 `push`。

## 2026-05-24 疑问：远端仓库必须先建吗？能否用命令直接创建？

- 时间：2026-05-24
- 现象：本地有提交，但不知道仓库名从哪里来，以及是否必须先在 GitHub 网页端创建空仓库。
- 结论：两种方式都可以。
  - 方式 A（推荐新手）：先在 GitHub 网页端创建空仓库，再本地 `remote add` + `push`。
  - 方式 B（命令行）：使用 GitHub CLI（`gh repo create`）可直接远程创建并推送。
- 原因说明：`git push` 需要一个“已存在的远端目标地址”；仓库名本质是你给远端仓库起的名字。
- 建议：当前学习阶段优先方式 A，最直观、排错成本最低。

## 2026-05-24 疑问：新建 GitHub 仓库时 Public/Private、README、.gitignore、License 怎么选？

- 时间：2026-05-24
- 现象：准备创建远端仓库，不确定初始化选项如何选择。
- 结论（当前 Hugo 博客项目）：
  1. 可见性：若目标是公开上线博客，推荐 Public。
  2. 初始化文件：当前本地仓库已有历史，GitHub 新仓库建议保持空仓库（不要勾选 README/.gitignore/License），避免首次 push 出现非快进冲突。
  3. .gitignore：应在本地项目维护（本项目已配置），用于忽略 `public/` 等构建产物。
  4. License：Public 仓库建议补充许可证；若不希望他人复用代码可暂不加（默认 all rights reserved），但会影响开源协作。
- 通用规则：
  - 不加 README：仅在纯个人短期仓库、无需说明入口时可省略。
  - 不加 .gitignore：仅在仓库几乎无构建产物/临时测试时可省略；工程项目通常都需要。
  - 加 License：当你希望明确别人“能否使用/修改/分发”你的代码或内容时应添加。

## 2026-05-24 疑问：为什么 Cloudflare 上线前通常先接 GitHub？

- 时间：2026-05-24
- 现象：已经完成 `remote add` 与 `push`，疑问是为何部署 Pages 前常走 GitHub。
- 结论：并非“必须”，而是“推荐默认路径”。
  - 路径 A（推荐）：Git 集成（GitHub/GitLab）=> 每次 push 自动构建与发布。
  - 路径 B：Direct Upload（手动上传 `public/`）=> 也能上线，但后续维护更手工。
- 原因说明：Git 集成的核心价值是可追溯（历史）、可回滚（版本）、可自动化（CI/CD）。
- 适用建议：学习和长期维护阶段优先 Git 集成；临时演示可用 Direct Upload。

## 2026-05-24 疑问：Cloudflare 上线后，失败的 Workers 项目能删除吗？后续如何维护？

- 时间：2026-05-24
- 现象：误走 Workers 项目导致失败，改走 Pages 后已成功上线。
- 结论：失败的 Workers 项目可以删除，且建议清理，避免后续混淆 Pages 与 Workers。

### 删除建议
1. 进入 Cloudflare Dashboard -> Workers & Pages。
2. 打开误建的 Workers 项目（不是 Pages 项目）。
3. 在 Settings/General 中找到删除入口（Delete/Remove）。
4. 如绑定过自定义域名或变量，先确认已迁移到 Pages 再删。

### 上线后维护（Hugo + Pages）
1. 日常发布：本地改内容 -> `git add/commit/push` -> Cloudflare 自动构建发布。
2. 预览分支：新功能先走分支，利用 Preview URL 验证后再合并。
3. 构建监控：每次发布看 Deploy Logs，重点看 build command 与 front matter 错误。
4. 内容治理：定期检查 `hugo list drafts/future/expired`，避免误发布或漏发布。
5. 配置治理：固定 `HUGO_VERSION`，大版本升级前先在分支验证。
6. SEO巡检：抽查 `/robots.txt`、`/sitemap.xml`、`/index.xml`、canonical。

## 2026-05-24 疑问：手机端打开长文时正文被截断，以及首篇文章时间是否应更早？

- 时间：2026-05-24
- 现象：`codex安装踩坑记录` 在手机端看起来只显示右半边；`Hello Hugo` 显示正常。另询问首篇文章时间是否应早于后续文章。
- 根因：单篇文章页使用 flex 布局时，长字符串/长代码会撑开文章最小宽度（flex item 默认 `min-width:auto`），导致移动端横向溢出与视觉截断。
- 解决方案：
  1. 在样式中为 `#single-post` 与文章容器补充 `min-width: 0`、`width: 100%`。
  2. 强化代码与长字符串处理：`pre/highlight` 横向滚动、行内 code 允许断行。
  3. 在移动端将单篇容器改为块布局，避免横向挤压。
  4. 将 `Hello Hugo` 日期调整为更早（`2026-05-21`），使时间顺序更符合“第一篇文章”的认知。
- 结论：上述改动可同时提升移动端可读性与文章时间线一致性。

## 2026-05-24 疑问：站点标题改为 Jiayi's Hugo Blog、是否可写搭建经历、Cloudflare 是否每次自动重建？

- 时间：2026-05-24
- 结论：
  1. 可以将站点顶栏标题改为 `Jiayi's Hugo Blog`（已修改 `hugo.toml` 的 `title`）。
  2. 完全可以把本次 Hugo 搭建经历整理成博文，这类“从 0 到 1 + 踩坑复盘”很有价值。
  3. 使用 Cloudflare Pages 的 Git 集成后，通常每次 `git push` 都会触发自动构建发布。
- 自动重建的边界：
  - 推送到 Production branch：生成正式线上版本。
  - 推送到其他分支：通常生成 Preview 链接（需在 Pages 项目中启用/保留该能力）。
- 常见误区：
  - 只改了本地但没 `git push`。
  - 推到了未连接的仓库或错误分支。
  - Cloudflare 项目连的是别的仓库/分支。

## 2026-05-24 疑问：第三篇博文标题、封面摘要优化、以及 Hugo 技术栈学习路线

- 时间：2026-05-24
- 诉求：
  1. 第三篇标题改短为“Hugo博客搭建复盘”。
  2. 增加封面图并优化开头摘要。
  3. 总结 Hugo 博客需要的技术栈，并给出学习路线图。
- 已处理：
  1. 标题已更新为 `Hugo博客搭建复盘`。
  2. 新增文章封面：`static/images/hugo-recap-cover.svg`，并在 front matter 通过 `title-images` 引用。
  3. 新增 `summary` 与 `<!--more-->`，让首页摘要显示更清晰。
  4. 构建验证通过。
- 学习结论：
  - Hugo 主要能力分层为：内容写作（Markdown）-> 站点配置（hugo.toml）-> 模板覆盖（layouts）-> 样式脚本（static）-> 发布流程（Git + Cloudflare Pages）。

## 2026-05-24 疑问：发布前是否先本地预览？关机重启后如何继续推送到同一仓库？

- 时间：2026-05-24
- 结论：
  1. 发布前强烈建议先本地预览：`hugo server -D --disableFastRender`。
  2. 关机重启后仍可继续使用 `git add/commit/push`，因为远端仓库地址保存在当前项目的 `.git/config`（`origin`），分支追踪关系也会保留。
- 说明：
  - 只要在同一个项目目录（`E:\Projects\personal-blog`）执行，Git 就知道推送到哪个远端。
  - 若想确认，执行 `git remote -v` 和 `git branch -vv`。
- 推荐日常流程：
  1. `cd E:\Projects\personal-blog`
  2. 本地预览并检查
  3. `git add .` -> `git commit -m "..."` -> `git push`

## 2026-05-24 疑问：第三篇博文为什么没显示？Git 是否会默认一直推到同一仓库？

- 时间：2026-05-24
- 现象：新文章已创建但站点列表中不显示。
- 根因：文章 `date` 在当前时间之后，被 Hugo 归入 future 内容，默认构建不会展示。
- 处理：将 `content/posts/hugo博客搭建复盘.md` 的 `date` 调整为过去时间（`2026-05-23T21:40:00+08:00`），并验证 `hugo list future` 已为空。
- 结论：
  1. 以后发布要“立刻可见”，`date` 不要晚于当前时间。
  2. Git 推送目标默认会沿用当前仓库的远端配置（`origin`）和分支跟踪关系；除非你手动改 `remote` 或切换到未设置上游的新分支。
- 自检命令：
  - `git remote -v`
  - `git branch -vv`

## 2026-05-24 疑问：年龄自动更新、标签管理优化、以及 AI 代运营可行性

- 时间：2026-05-24
- 诉求：
  1. 侧栏 age 能否按生日自动更新（生日 2003-07-17）。
  2. 标签太零散，是否应合并（如“踩坑”和“踩坑复盘”），首篇是否可以不打标签。
  3. 未来能否让 Codex 作为 agent 持续更新博客（如每周收集论文并生成博文）。
- 已处理：
  1. 新增 `layouts/shortcodes/age.html`，侧栏 `age` 改为 `{{< age birth="2003-07-17" >}}` 自动计算。
  2. `hello-hugo.md` 取消标签；`hugo博客搭建复盘.md` 标签归并为“踩坑”；模板改为“仅有标签时显示 tags 行”。
- 策略结论：
  - 标签治理建议：控制总量、保持稳定词表、优先复用已有标签、每篇 2-4 个高信息密度标签。
  - Agent 代运营可行，但建议“半自动”：自动收集候选 -> 你审核 -> 再发布，避免错误信息直接上线。
