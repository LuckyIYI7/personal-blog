# Hugo 安装后命令不可用（hugo: CommandNotFoundException）

## 现象
- 安装完成后提示已修改 PATH 和添加别名 `hugo`
- 但执行 `hugo version` 报错：无法识别 hugo 命令

## 根因
- 安装器修改了系统/用户 PATH
- 当前 PowerShell 进程仍使用旧环境变量，未重新加载 PATH

## 调试流程
1. `winget list --id Hugo.Hugo.Extended` 确认已安装
2. `where.exe hugo` / `Get-Command hugo -All` 检查命令解析
3. 关闭并重开终端，再执行 `hugo version`

## 常见误区
- 装完不重开终端
- 同时存在多个 `hugo.exe` 导致混淆
- 只在一个终端里反复尝试，不验证 PATH 是否刷新

## 解决方案
- 关闭当前 PowerShell，重新打开
- 再执行 `hugo version` 验证



# Hugo 安装位置错了（想装到E盘）

## 现象
- 安装完成后显示在C盘（C:\Users\64169\AppData\Local\Microsoft\WinGet\Packages\Hugo.Hugo.Extended_Microsoft.Winget.Source_8wekyb3d8bbwe\hugo.exe）

## 根因
- 在全局powershell操作

## 解决方案

1. 先尝试让 winget 安装到 E 盘

```
winget uninstall --id Hugo.Hugo.Extended 
winget install --id Hugo.Hugo.Extended --location E:\DevTools\Hugo
```

2. 重开 PowerShell，验证

```
where.exe hugo hugo version
```

3. 确认只有一个 Hugo（避免 PATH 冲突）

```
Get-Command hugo -All
```


# 本地预览连接失败（localhost:1313 无法访问）

## 现象
- 浏览器访问 `http://localhost:1313` 连接失败
- 终端中未看到 Hugo 服务可用提示

## 根因
- `hugo server` 没有启动，或启动后终端被关闭
- 端口不一致（例如服务跑在 1314，浏览器仍访问 1313）

## 调试流程
1. 在项目根目录执行：`cd E:\Projects\personal-blog`
2. 启动服务并观察日志：`hugo server -D --bind 127.0.0.1 --port 1313`
3. 保持终端窗口不关闭，访问：`http://127.0.0.1:1313`
4. 若仍失败，改端口重试：`hugo server -D --port 1314`

## 常见误区
- 把“连接失败”和“页面 404”当成同一个问题
- 启动后立即关闭终端，导致服务进程结束
- 不看终端输出就直接反复刷新浏览器

## 解决方案
- 在项目根目录重新启动 Hugo 本地服务
- 使用与服务一致的地址和端口访问
- 先确认终端出现 `Web Server is available at` 再打开浏览器


# Shortcode 已添加但页面无变化（`Raw HTML omitted`）

## 现象
- 文章末尾已加入 `note` shortcode，但页面没有出现提示框样式
- 构建日志出现：`WARN Raw HTML omitted while rendering ...`

## 根因
- 文章中使用了 `%` 语法：`{{% note %}}...{{% /note %}}`
- 当前项目未开启 `markup.goldmark.renderer.unsafe`
- `note` shortcode 输出的是 HTML，进入 Markdown 渲染阶段后被 Goldmark 当作不安全原始 HTML 丢弃

## 调试流程
1. 检查 shortcode 文件是否存在：`layouts/shortcodes/note.html`
2. 检查文章中 shortcode 调用语法
3. 运行构建并观察日志：`hugo` 或 `hugo server -D`
4. 查看生成页源码是否出现 `<!-- raw HTML omitted -->`

## 常见误区
- 以为“shortcode 语法无报错 = 一定渲染成功”
- 把 `%` 和 `<` 两种 shortcode 调用方式混用而不区分渲染阶段
- 只看页面外观，不看构建日志里的关键 warning

## 解决方案
- 将调用改为角括号语法（适配当前安全配置）：
  - `{{< note type="warn" title="踩坑提醒" >}}`
  - `{{< /note >}}`
- 保持 `note.html` 和 `main.css` 不变，重新构建验证


# 时间线状态色不明显、`codex` 未按 Markdown 渲染

## 现象
- 时间线已显示，但 `warn/ok/error` 状态颜色几乎看不出差异
- 时间线文本里的 `` `codex` `` 直接显示为反引号文本，没有渲染为 `<code>`

## 根因
- CSS 里存在旧规则 `.timeline-body li::before`，其选择器优先级高于 `.timeline-item-warn::before` 等状态规则，导致状态色被覆盖
- `titem` shortcode 使用 `{{ .Inner }}` 直接输出正文，未经过 `markdownify`

## 调试流程
1. 检查文章生成后的 HTML，确认是否存在 `timeline-item-warn/ok/error` 类
2. 检查 `titem` 输出是否包含 `<code>codex</code>`（用于判断 Markdown 是否被渲染）
3. 检查 CSS 中是否有更高优先级规则覆盖状态颜色
4. 重新构建并复查生成页面

## 常见误区
- 只看“类名存在”就认为样式一定生效，忽略了 CSS 优先级
- 在 shortcode 内直接输出 `.Inner`，忘记需要 `markdownify` 才能处理内联 Markdown
- 只修功能不修视觉，导致“看起来像没变化”

## 解决方案
- 将 `titem` 正文改为：`{{ .Inner | markdownify }}`
- 删除旧的 `.timeline-body li::before` 相关旧规则，改用 `timeline-item-*` 规则控制状态
- 增强状态视觉：为 `ok/warn/error` 增加不同背景色与边框色
- 同步优化正文排版：增加行内代码样式和表格样式，提高可读性


# Markdown 表格只显示左半部分，右半区域空白

## 现象
- 表格外层出现完整边框
- 但表格内容集中在左侧，右侧出现较大空白，看起来像被截断

## 根因
- 使用了 `article table { display: block; overflow-x: auto; }`
- `display: block` 破坏了标准表格布局算法，导致表头/表体宽度行为异常

## 调试流程
1. 检查表格样式是否包含 `display: block`
2. 对比生成页面中的 `<table>` 结构，确认不是 Markdown 内容缺失
3. 将表格恢复为标准布局后重新构建验证

## 常见误区
- 以为“给 table 加 block + overflow”一定是通用响应式方案
- 表格显示异常时先怀疑 Markdown 内容，而忽略了 CSS 布局上下文

## 解决方案
- 移除 `display: block` 和 `overflow-x: auto`
- 改为标准表格布局：`table-layout: fixed; border-collapse: collapse; width: 100%`
- 补充单元格竖向分隔线和末行边界处理，提升可读性和稳定性


# 代码块长行溢出（路径跑出黑框）与代码块样式呆板

## 现象
- 代码块中的长路径（如 `source = '\\?\C:\...`）超出黑色代码框边界
- 代码框视觉较生硬，阅读体验一般

## 根因
- 代码块默认 `pre` 行为不自动换行，长字符串会保持单行
- Chroma 输出中包含行包装 `display:flex` 的内联结构，进一步放大溢出问题
- 当前代码块缺少统一视觉样式（圆角、边框、层次）

## 调试流程
1. 检查代码块样式是否仅有默认 `pre` 行为
2. 查看生成 HTML 中语法高亮结构（`<div class="highlight">...<span style="display:flex">`）
3. 增加换行与溢出控制后重新构建验证长路径显示

## 常见误区
- 只给外层容器加样式，不处理 `pre/code` 的换行规则
- 忽略语法高亮器生成的内联结构对布局的影响

## 解决方案
- 为 `pre`/`.highlight pre` 启用安全换行：`white-space: pre-wrap`、`overflow-wrap: anywhere`
- 对语法高亮行包装结构强制 `display: block`，避免 flex 行溢出
- 同步美化代码块：深色底、圆角、边框、内阴影与更舒适的内边距


# TOC 改模板后报错：`apply base template failed: unexpected EOF`

## 现象
- 访问博客时报错：
  - `process: ".../layouts/_default/baseof.html:54:1": apply base template failed: template: single.html:54: unexpected EOF`

## 根因
- `layouts/_default/single.html` 中 `{{ define "main" }}` 模板块没有正确闭合
- 文件末尾少了对应的 `{{ end }}`

## 调试流程
1. 读取报错信息中的“实际报错模板和行号”（本次是 `single.html:54`）
2. 打开目标模板，逐个检查 `{{ if/with/range/define }}` 是否都有成对 `{{ end }}`
3. 检查 HTML 标签是否有未闭合（避免误导排查）
4. 修复后执行 `hugo --cleanDestinationDir` 验证

## 常见误区
- 报错里出现 `baseof.html` 就误以为根因在 base 模板
- 插入脚本或 TOC 片段后忘记补 `{{ end }}`
- 同时改动太多块结构，导致难以定位“哪一层没闭合”

## 解决方案
- 重写并整理 `single.html`，保证模板结构清晰
- 确保 `define`、`if`、`with` 块全部闭合
- 重新构建确认页面恢复正常


# OpenGraph 模板报错：`partial "templates/opengraph.html" not found`

## 现象
- 访问任意页面时出现构建失败提示
- 报错信息：
  - `error calling partial: partial "templates/opengraph.html" not found`

## 根因
- 在 `baseof.html` 中使用了 `partial "templates/opengraph.html"` 调用方式
- 但项目里并没有 `layouts/partials/templates/opengraph.html` 这个 partial 文件
- Hugo 因找不到该 partial，导致整站渲染失败

## 调试流程
1. 查看报错中的模板名和调用类型（本次是 `partial`）
2. 检查 `layouts/partials/` 下是否存在对应文件
3. 对照 Hugo 内置模板调用方式进行修正
4. 重新执行 `hugo --cleanDestinationDir` 验证

## 常见误区
- 把“主题里可用的 partial 路径”直接用于无主题/自建模板项目
- 混淆 `partial`（调用项目内文件）和 `template`（可调用 Hugo 内置模板）

## 解决方案
- 将 `baseof.html` 中调用改为 Hugo 内置模板：
  - `{{ template "_internal/opengraph.html" . }}`
  - `{{ template "_internal/twitter_cards.html" . }}`
- 重新构建后页面恢复正常，`robots.txt` 和 `404.html` 可访问

# 2026-05-23 主题迁移 Step 1：Git 与目录操作报错

## 现象
- 执行 git add . 时出现：warning: adding embedded git repository: themes/neopost
- 执行 Rename-Item .\\layouts .\\layouts.local-backup 时出现：访问被拒绝

## 根因
- 	hemes/neopost 是独立 Git 仓库（含 .git），外层仓库会把它当作嵌套仓库（gitlink），不是普通文件目录
- layouts 目录在重命名时被进程占用（常见为 hugo server 未关闭）

## 调试流程
1. 先看 Git 提示是否包含 embedded git repository/submodule 关键字
2. 用 git status --short 观察是否出现 160000 模式（gitlink 特征）
3. 关闭 Hugo 进程后再重试目录改名：Get-Process hugo | Stop-Process -Force
4. 复查目录改名结果与仓库状态

## 常见误区
- 直接 git add . 后忽略 nested repo 警告
- 没有停止 hugo server 就改动 layouts/ 目录
- 把“主题源码目录”与“子模块管理方式”混为一谈

## 解决方案
1. 改为普通目录跟踪主题源码：
   - git rm --cached themes/neopost
   - 删除主题内 .git：Remove-Item -Recurse -Force .\\themes\\neopost\\.git
   - git add themes/neopost && git commit
2. 停止 Hugo 进程后再执行目录重命名，问题解决

# 2026-05-23 主题迁移 Step 2：shortcode 模板缺失导致构建失败

## 现象
- 执行 hugo server -D --disableFastRender 报错：
  - ailed to extract shortcode: template for shortcode "timeline" not found

## 根因
- 为了验证主题，已将项目原有 layouts/ 重命名为 layouts.local-backup/
- 文章 content/posts/codex安装.md 仍使用自定义 shortcode：	imeline / 	item / 
ote
- 
eopost 主题没有这三个 shortcode，因此 Hugo 在解析正文时失败

## 调试流程
1. 读取报错末尾关键信息，先锁定是 shortcode not found（不是 Markdown 语法问题）
2. 检查触发文件：content/posts/codex安装.md
3. 检查现有 shortcodes：	hemes/neopost/layouts/shortcodes 与 layouts.local-backup/shortcodes
4. 确认自定义 shortcode 只在备份目录中，当前生效路径不存在

## 常见误区
- 以为“主题切换只影响页面样式”，忽略正文对 shortcode 的依赖
- 只迁移 hugo.toml，没迁移自定义 shortcode 资产
- 看到大量日志后忽略最后一行最关键错误

## 解决方案
- 最小修复：恢复项目级 layouts/shortcodes/，把 	imeline.html、	item.html、
ote.html 从 layouts.local-backup/shortcodes/ 复制回来
- 这样既保留 neopost 主体模板，又能兼容旧文章中的 shortcode 调用

# 2026-05-23 主题迁移 Step 3：posts 级联配置 TOML 解析失败

## 现象
- 启动 hugo server -D --disableFastRender 报错：
  - content/posts/_index.md:3:1: toml: invalid character at start of key: \\

## 根因
- content/posts/_index.md 第 3 行写成了 \\[cascade]
- TOML front matter 中分组必须写为 [cascade]，前导反斜杠会导致解析失败

## 调试流程
1. 根据报错定位到具体文件和行号（_index.md:3:1）
2. 打开文件逐行检查 front matter 语法
3. 修正分组声明后重新启动 Hugo 验证

## 常见误区
- 从聊天/文档复制时误带转义符（\\）
- front matter 块里混入 Markdown 转义习惯
- 只看警告不看最后一行致命错误

## 解决方案
- 将 \\[cascade] 改为 [cascade]
- 保持 TOML front matter 结构完整：起止 +++ 成对

# 2026-05-23 主题迁移 Step 3：TOC 解析器要求标题必须带 id

## 现象
- 在修复 content/posts/_index.md TOML 后，构建报错：
  - The "toc-parse-content" partial detected that the "安装关键节点" heading does not have an ID attribute

## 根因
- 
eopost 的 TOC partial 会解析页面中 h2~h6，并要求每个标题都带 id
- 文章中的 	imeline shortcode 输出了 <h3 class="timeline-title">...，没有 id

## 调试流程
1. 先确认 TOML 已修复并重新构建
2. 读取新的首条致命错误，定位到缺失 id 的标题文本
3. 反查 shortcode 模板，确认 	imeline.html 的 h3 未设置 id
4. 给标题增加可稳定生成的锚点 id 后复构建验证

## 常见误区
- 只关注 Markdown 标题，忽略 shortcode 生成的 HTML 标题
- 看到 TOC 报错就误判为文章语法错误

## 解决方案
- 更新 layouts/shortcodes/timeline.html：
  - 增加 {{  :=  | anchorize }}
  - 标题改为 <h3 id="{{  }}" ...>
- 重新构建通过（仅剩 deprecation 警告）

# 2026-05-23 关闭侧栏 tags 时触发的 hugo.toml 编码/字符串错误

## 现象
- 修改 hugo.toml 后，Hugo 报错：
  - invalid character at start of key: ï
  - asic strings cannot have new lines
  - literal strings cannot have new lines

## 根因
- 文件编码与内容同时受损：
  1. 写入过程中引入了 UTF-8 BOM，导致文件首字符异常
  2. 原配置中多处字符串已损坏（引号不闭合/跨行），在重新写入后被 Hugo 严格解析出来

## 调试流程
1. 通过报错行号定位 hugo.toml
2. 检查编码与逐行内容，确认字符串闭合问题
3. 将配置文件重写为 UTF-8（无 BOM）且结构完整
4. 重新构建验证

## 解决方案
- 重写 hugo.toml 为干净可解析版本
- 保留并确认  [params.sidebar.tags] enable = false  生效
- 构建通过，首页侧栏 tags 区块已隐藏

# 2026-05-23 回收站脚本首版解析失败（字符串编码导致引号损坏）

## 现象
- 执行 `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\trash.ps1 list` 时出现多个 `Unexpected token` / `string terminator` 解析错误。

## 根因
- 首版脚本包含大量中文提示文本，在当前终端编码链路下部分字符串引号被破坏，导致 PowerShell 解析失败。

## 调试流程
1. 根据报错行号检查脚本对应行。
2. 逐行确认字符串是否有闭合引号。
3. 发现多处提示字符串在编码转换后损坏。

## 解决方案
- 将脚本提示文本改为 ASCII/英文，避免编码链路导致的字符串损坏。
- 以 UTF-8（无 BOM）重写脚本后重新执行，`list` 动作通过。

# 2026-05-23 首页文章中文显示乱码（meta charset 位置过后）

## 现象
- 首页两篇文章中文出现乱码。

## 根因
- `layouts/_default/baseof.html` 中 `<meta charset="utf-8">` 放在了大量内联 `<style>` 之后。
- 部分浏览器在编码探测阶段会先按本地默认编码解析，导致 UTF-8 内容显示乱码。

## 调试流程
1. 检查生成文件 `public/index.html` 头部，确认 `<meta charset>` 位置。
2. 发现 `charset` 在大段 CSS 之后（超出安全探测窗口）。
3. 将 `charset` 提前到 `<head>` 开头并重新构建验证。

## 解决方案
- 调整 `layouts/_default/baseof.html`：把 `<meta charset="utf-8">` 移到 `<head>` 内最前位置。
- 重建后首页中文恢复正常。

# 2026-05-23 首页出现 `?button>` 残片与按钮异常

## 现象
- 首页文章区原 TOC 按钮异常，页面出现类似 `?button>` 的残片。

## 根因
- `layouts/index.html` 中按钮标签被破坏：`</button>` 缺失 `<`，变成了文本残片。

## 调试流程
1. 检查首页模板 `layouts/index.html`。
2. 定位到按钮行，发现 malformed closing tag。
3. 修复为标准按钮标签并重建验证。

## 解决方案
- 将按钮改为稳定文本 `TOC`，并使用正确闭合：`<button ...>TOC</button>`。
- 重建后首页按钮恢复正常，残片消失。

## 2026-05-24 Cloudflare 部署失败：`npm error could not determine executable to run`

- 时间：2026-05-24
- 现象：Cloudflare 构建日志显示 `Executing user deploy command: npx wrangler deploy`，随后在 build 阶段执行 `npx hugo` 失败，报 `npm error could not determine executable to run`。

### 根因
1. 触发的是 **Workers Builds 自动配置链路**（`wrangler deploy`），不是 Hugo 常用的 **Pages Git 构建链路**。
2. 自动检测给出的构建命令是 `npx hugo`，但 Hugo 不是 npm 可执行包，因此 `npx` 无法解析可执行入口。

### 调试流程（可复用）
1. 先看第一条关键日志：
   - 若出现 `Executing user deploy command: npx wrangler deploy`，说明当前是 Workers 部署链路。
2. 再看 build command：
   - `npx hugo` 基本可判定错误；Hugo 在 Pages 里应使用 `hugo`（可加 `-b $CF_PAGES_URL`）。
3. 最后对照目标平台：
   - 静态博客优先 `Pages -> Import Git repository`。

### 解决方案
- 推荐修复（Pages）：
  1. 在 Cloudflare 选择 `Pages` 项目（不是 Workers）。
  2. Build command 改为：`hugo --gc --minify -b $CF_PAGES_URL`
  3. Output directory：`public`
  4. 环境变量：`HUGO_VERSION=0.161.1`
- 备选修复（Workers）：保留 `wrangler deploy` 链路并自行维护 `wrangler.jsonc`，但学习成本更高，不是本项目首选。

### 常见误区
- 把 Pages 与 Workers 的创建入口混用。
- 看到 `npx wrangler deploy` 仍按 Pages 参数排错，方向会偏。
